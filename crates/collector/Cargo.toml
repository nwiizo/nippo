[package]
name = "nippo"
version = "0.1.7"
edition = "2024"
description = "Claude Code session collector for daily reports and reflection"
license = "MIT"
repository = "https://github.com/nwiizo/nippo"
keywords = ["claude", "daily-report", "reflection", "productivity"]
categories = ["command-line-utilities"]

[[bin]]
name = "nippo"
path = "src/main.rs"

[features]
# Interactive read-only ledger dashboard. Off by default so the headless
# `nippo ledger` path (used by the skill) has no extra dependencies.
tui = ["dep:ratatui"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
rayon = "1"
glob = "0.3"
chrono = { version = "0.4", features = ["serde"] }
iana-time-zone = "0.1"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
rusqlite = "0.37"
ratatui = { version = "0.30", optional = true }

[dev-dependencies]
tempfile = "3"
